// Адрес сервера админки. Заполняется один раз после того, как воркер выложен
// (см. worker/README.md), и коммитится в репозиторий: секретного тут нет —
// это просто адрес, а всё, что за ним, закрыто входом.
//
// Пока строка пустая, редактор работает без сервера: правки хранятся в
// браузере, публикация недоступна, картинки выбираются из уже загруженных.
// Это не «сломано», а рабочий режим — в нём можно спокойно всё показывать.

export const API_BASE = 'https://kunaev-admin.mas040500.workers.dev';
